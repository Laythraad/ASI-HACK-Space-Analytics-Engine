# المشكّلات التي تم حلّها

**ASI-HACK Space Analytics Engine — MORS Command Center**
ملف توثيقي لكل خلل رصدناه في **البيانات وتحليلها** ثم أصلحناه، مع الدليل الرقمي قبل/بعد واسم فحص الأتمتة الذي يثبت الحالة بعد كل تعديل.

- آخر تحديث: **2026-09-25**
- حالة التحقّق الحالية: `AUDIT: 225 checks, 0 failed` → `RESULT: AUDIT: PASS`
- حالة الاختبار الذاتي: `SELFTEST : PASS ✅` (`python main.py --selftest`)

---

## 1) كيف تُقرأ هذه الملفّات

كل مشكلة بنفس التركيبة الخمسة:

| الحقل | المعنى |
|---|---|
| **المُلاحظة** | ما الذي كان خاطئاً في السلوك/الرقم، بالأرقام |
| **السبب الجذري** | سطر/منطق الخلل في الكود |
| **الإصلاح** | ما الذي تغيّر فعلياً |
| **الرقم بعد الإصلاح** | القيمة الجديدة القابلة للقياس |
| **التحقّق** | اسم فحص `tools/audit_data.py` الذي يمنع عودة الخلل |

**كيفية إعادة التحقّق يدوياً:**

```powershell
python main.py                 # شغّل الخادم
python tools/audit_data.py     # → RESULT: AUDIT: PASS
python main.py --selftest      # → SELFTEST : PASS ✅
```

---

## 2) مشكّلات هذه الجولة (التركيز: البيانات والتحليل)

### P-01 — الوحدة الخاطئة لسرعة القمر الصناعي (m/s معروضة كـ km/s)

- **المُلاحظة:** القمر LEO يظهر بسرعة `≈7546 km/s` (خطأ ×1000) — رقم فيزيائياً مستحيل (القيمة الحقيقية ≈ 7.6 km/s).
- **السبب الجذري:** `agents/mors_data.py:834,836,1098` — الصيغة `sqrt((G_EARTH / 1000.0) / a)` تحوّل `G` إلى وحدات غير متسقة مع نصف المحور بالكيلومتر.
- **الإصلاح:** استبدال بـ `G_EARTH / 1e9` (تحويل m³/s² → km³/s²) في المسارين: `_orbit_from_mean_motion` ومصدر الأقمار المحلي.
- **بعد الإصلاح:** `velocity_kms ∈ [3.06 … 7.81]`.
- **التحقّق:** `orbital velocity plausible (1.5-11.5 km/s)` + `velocity matches sqrt(GM/a) within 12%`.

### P-02 — apogee/perigee = 0 عند غياب القيم في مصدر TLE

- **المُلاحظة:** صفوف بأوج/حضيض `0 km` بينما `altitude_km > 0` — تناقض مباشر يُبطل مقارنة المدار.
- **السبب الجذري:** مصدر البيانات لا يعيد `apogee/perigee` دائماً وكانت القيم تُمرَّر كما هي (0).
- **الإصلاح:** `agents/mors_data.py:865,878` — اشتقاق `a(1±e) − R_earth`، واستخدام قيم الكتالوج فقط عندما تكون متسقة (`apogee ≥ perigee > 0`).
- **بعد الإصلاح:** مثال NORAD 69100 → `apogee 533.93 / perigee 531.65 km`.
- **التحقّق:** `apogee/perigee > 0 on every row` + `apogee >= perigee`.

### P-03 — CelesTrak يرفض الطلبات (HTTP 403) ومصدر الأقمار كله ميّت

- **المُلاحظة:** كل المسارات/النطاقات لدى CelesTrak ترجع `403` → كان المصدر يسقط إلى مجموعة افتراضية غير مُعلَنة.
- **السبب الجذري:** حظر IP/معدل على الطرف الخارجي، ولم يكن للمحرك بديل حيّ.
- **الإصلاح:** `agents/mors_data.py:962,1018` — ترجمة `_sat_tle_api()` (مرآة `https://tle.ivanstanojevic.me/api/tle/`، بلا مفتاح، ترتيب شعبيّة، حجم صفحة ≤100، تحليل عمودي TLE من `line1/line2`)، مع `_sat_celestrak()` أولاً ثم `_sat_local()` كخيار مُعلَن، وموازنة الفئات `_SAT_BUDGET = {LEO:24, MEO:12, GEO:14}`، وتصنيف أدقّ `_orbit_class` (GEO بين 35000–36500 كم و`e<0.15` فقط، وإلا `None` لاستبعاد المدارات العالية/الأفقية).
- **بعد الإصلاح:** `50 قمراً (24 LEO / 12 MEO / 14 GEO)`، `via=api`، النقطة النهائية في `trace.endpoint` بصدق.
- **التحقّق:** `50 satellites returned` + `live satellite source names its endpoint` + `altitude agrees with orbit class`.

### P-04 — عدّاد الانبعاثات الحلقية (CME) كان يضمّ بقع الشمس (FLR)

- **المُلاحظة:** `cme_count = 142` بينما اللوحة تُظهر `125` انفجاراً حلقياً فقط — الرقم مبالغ فيه لأن سجلات FLR (الانبعاثات) كانت مُحتسبة ضمن CME.
- **السبب الجذري:** `agents/agent1_ingestion.py:522-527` — `len(donki_df)` يعدّ كل صفوف DONKI.
- **الإصلاح:** `int((donki_df["event"] == "CME").sum())` + إضافة `donki_flare_count` المنفصل (17 سجلاً).
- **بعد الإصلاح:** `cme_count = 125`، `flare_records = 17`.
- **التحقّق:** `report.metrics.cme_count == space-weather CME total` + `space-weather panel is CME-only`.

### P-05 — `solar_wind_avg_kms` كان يحمل متوسط سرعة الانبعاثات الحلقية

- **المُلاحظة:** `solar_wind_avg_kms = 482.5` وهو متوسط سرعات CME، لا الرياح الشمسية (مقياس OMNI ≈ 412 km/s).
- **السبب الجذري:** نفس متغيّر دونكي كان يُعاد استخدامه في مقياس مختلف.
- **الإصلاح:** `agents/agent1_ingestion.py:528-536` — القراءة من المرجع المحلّي `data/reference_metrics.csv` عبر `_reference_value("solar_wind_speed_avg", 412.0)`، وإظهار متوسط CME في متغيّر مستقل `cme_speed_mean_kms`.
- **بعد الإصلاح:** `solar_wind_avg_kms = 412.0` و`cme_speed_mean_kms = 482.5` (مقياسان مختلفان، كلٌّ باسمه).
- **التحقّق:** `solar_wind_avg_kms is an OMNI-scale value (250-900 km/s)` + `cme_speed_mean_kms plausible (100-3000 km/s)`.

### P-06 — إسقاط صامت لبعض الانبعاثات الشمسيّة (FLR)

- **المُلاحظة:** 17 سجلاً يصل من DONKI لكن اللوحة تعرض 12 فقط دون تفسير.
- **السبب الجذري:** الصفوف بلا فئة مُصنَّفة كانت تُهمل داخل `_fetch_flares`.
- **الإصلاح:** `agents/datasets.py:415` — الدالة تُعيد `(records, live, stats)` وتُعرَّض في مصدر `kp-index`: `flare_records=17`، `flare_classified=12`، `flare_unclassified=5`، `flare_window`.
- **بعد الإصلاح:** `12 + 5 = 17` (لا فقدان بلا أثر).
- **التحقّق:** `flare stats reconcile (classified + unclassified == records)` + `no silent drops are hidden (unclassified is reported)`.

### P-07 — مؤشّر صحّة الأوج habitability_index كان صفراً لكل الكواكب

- **المُلاحظة:** `59/60` كوكباً بقيمة `0.0` (ورقم واحد `null`) — العمود الأهم علمياً لم يُنقل أي معلومة.
- **السبب الجذري:** `agents/mors_data.py:_exo_row` — المعادلة `S = (T_eq/255)⁴ · (R_p/R_E)² / a²` تجعل **نصف قطر الكوكب** (لا الإشعاع) يهيمن على النتيجة فتقفز `S` إلى ملايين، والبوابات `<1.1 / <3.0` لا تطابق Kopparapu 2013.
- **الإصلاح:**
  - `S = pl_insol` (إشعاع الأرضي الحقيقي من NASA Exoplanet Archive)، مع بديل `(T_eq/255)⁴` عند غيابه — **نصف القطر أُزيل من المعايير** لأن المنطقة الصالحة للحياة خاصية إشعاع النجم.
  - بوابات: `S<1.1 → 1.0` (جريان شبيه بالبيئة الرابعة)، `S<1.78 → 0.83` (الحافة الداخلية — حد فينوس الحديثة)، وإلا `0.0`.
  - استعلام TAP مقسوم: `45` أحدث كوكباً + `15` كوكباً داخل `0.25 ≤ S ≤ 1.8` حتى يظهر التحليل على بيانات حقيقية، مع إزالة التكرارات.
  - `habitable_zone_note` صار يشرح السبب بدقة لكل صف.
- **بعد الإصلاح:** `14 كوكباً = 1.0`، `1 = 0.83`، `43 = 0.0`، `1 = null` (بيانات ناقصة صريحة) — منها **Kepler-62 f** و**55 Cnc f**.
- **التحقّق:** `habitability gate reproduces from S (Kopparapu 2013)` + `at least 5 planets inside the habitable-zone window` + `no habitability value without an S value`.

### P-08 — مسار الأرض والمريخ مختلط داخل سلسلة واحدة باسم «Earth»

- **المُلاحظة:** `/api/data/horizons` أعطى `1442 نقطة` بمدى شمسية `1.003 – 1.5508 au` بعنوان `body: "Earth"` — والأرض لا تتجاوز `1.02 au` أبداً؛ ورسم المدار كان يرسم خطاً يعبر بين مدارين.
- **السبب الجذري:** `main.py:_horizons_series` كان يبني سلسلة واحدة من كل صفوف Horizons (أرض + مريخ) ويتبنّى `body` من أول صف فقط.
- **الإصلاح:** `main.py:317,328` — تقسيم `orbits = {"Earth": [...], "Mars": [...]}` مع `bodies` و`body` للجسم الأساسي و`series` = مسار جسم واحد فقط؛ و`static/index.html:561` — `drawOrbit` يرسم مجموعة بيانات لكل جسم مع مفتاح ألوان.
- **بعد الإصلاح:** `Earth 721 نقطة (1.003–1.0106 au)` و`Mars 721 نقطة (1.35–1.75 au)` كلٌّ في مساره.
- **التحقّق:** `series is exactly one body's track (no cross-planet line)` + `horizons/Earth: heliocentric radius inside 0.95-1.05 au` + `horizons/Mars: heliocentric radius inside 1.35-1.75 au`.

### P-09 — أعداد المدرّجات الحراريّة في الكمية خاطئة ×100

- **المُلاحظة:** `E₀` معروضاً `266.8 cm⁻¹` بينما القيمة الصحيحة `≈26,680 cm⁻¹` — السلّم (ω = 10¹⁶ rad/s) يقع في المرئي/الفوق بنفسجي لا في الأشعة تحت الحمراء البعيدة.
- **السبب الجذري:** `agents/mors_data.py:1675` — `x / (1.98644586e-23 * 100.0)`؛ قسمة إضافية على 100 إلى جانب `h·c`.
- **الإصلاح:** القسمة على `h·c = 1.98644586e-23 J·cm` فقط.
- **بعد الإصلاح:** `26680.8178, 79539.0417, 132900.6772 … cm⁻¹` ومطابقة `E_J/(h c)` بنسبة `<1%`.
- **التحقّق:** `mors QHO levels_cm-1 == E_J/(h c)`.

### P-10 — منحنى تذبذب رابي (Rabi) كان خطاً أفقياً عند الصفر

- **المُلاحظة:** `41` عيّنة كلها `t_ns = 0.0` و`p_excited = 0.0` — لا منحنى إطلاقاً.
- **السبب الجذري:** `agents/datasets.py:683` — خطأ وحدتان معاً: المحور `round(t, 3)` على قيمة بالثواني (كلها <0.0005 → 0)، والدالة `sin(ω · t · 1e-9 / 2)` تعيد ضرب الزمن ثانياً فتصبح الحدود `≈10⁻⁸`.
- **الإصلاح:** `t_ns = t * 1e9` و`sin(ω·t/2)` على الثواني الخام.
- **بعد الإصلاح:** المحور `0.0 → 4.0 ns` بخطوة 0.1، وال.population `0 → 1` (أربعة تذبذبات كاملة).
- **التحقّق:** `rabi axis in nanoseconds (0 -> 4 ns)` + `rabi time strictly increasing` + `rabi population spans a full oscillation (0 -> 1)`.

### P-11 — مرحلة الطيف الضوئي `phase = 1.0` تخالف تعريف `mod 1`

- **المُلاحظة:** نقطة واحدة على الأقل لكل منحنى بقيمة `1.0` بينما التعريف المعلن `φ = ((t − t₀)/P) mod 1` → `[0,1)`.
- **السبب الجذري:** `agents/mors_data.py:1263` — المرحلة مأخوذة من نهاية المصفوفة دون لفّها.
- **الإصلاح:** `phase = t % 1.0` (النقطة المطابقة للدورة التالية تصير `0.0`).
- **رقم إضافي للصدق:** أهداف غير دورية (`variability_period_d = null`) كانت تعرض `period_d = 1.0` كأنها فترة مقيسة → أُضيف `period_source = "measured"` أو `"default 1.0 d folding window (aperiodic target)"` (`agents/mors_data.py:1259`).
- **التحقّق:** `light curves: phase/flux/depth/contacts consistent` + `light-curve period mirrors its photometry target` + `aperiodic targets declare a null period`.

### P-12 — أولويّة البطاقات: العتبات لا تطابق المعادلة الموثّقة

- **المُلاحظة:** بطاقة `P-005` بدرجة `73.8` مُعلَّمة `High` بينما الوثائق تقول `High ≥ 75` — وكلّ `by_priority` كان يرث الخطأ.
- **السبب الجذري:** `agents/mors_data.py:216-217` — العتبات `70/40` بدل `75/50`.
- **الإصلاح:** العتبات الآن `≥75 → High`، `≥50 → Medium`، وإلا `Low` بنفس معادلة `0.40·evidence + 0.35·impact + 0.25·confidence`.
- **التحقّق:** `<id> priority score == 0.40e+0.35i+0.25c` و`<id> priority label matches thresholds (75/50)` لكل بطاقة + `by_priority tallies the cards`.

### P-13 — صفوف الكواكب بإحداثيات فارغة دون تفسير

- **المُلاحظة:** المريخ/المشتري بـ `ra_deg = null` و`dec_deg = null` دون أي توضيح في الواجهة.
- **السبب الجذري:** موضع الكوكب يعتمد على Epoch ولا يُخزَّن كرقم ثابت.
- **الإصلاح:** `agents/mors_data.py:1501,1509` — حقل `position_note` يشرح السبب، والفحص يفرّق بين الصفوف الفلكية (يجب أن تحمل RA/Dec) وصفوف الكواكب (عناصر مدارية + ملاحظة).
- **التحقّق:** `non-planetary rows carry in-bounds RA/Dec` + `planet rows are elements-only (null RA/Dec + semimajor_au)` + `planet rows explain their null positions`.

### P-14 — لوحة «الفريق» كانت بأسماء أدوار عامة بدل أسماء الفريق

- **المُلاحظة:** أسماء وهمية/أدوار بصيغة نائب في حلّ المقترحات والبحث.
- **الإصلاح:** `agents/mors_data.py:1928` — قائمة الفريق الأربعة الحقيقية (أدناه) مع `name_latin / title / role / skills / owns`، وخريطة `owner` في `solutions` و`research` على الأسماء نفسها.
- **التحقّق:** `team is the four-member submission roster` + `module ownership covers the 6 MORS domains`.

### P-21 — عدّاد «الشروط المغطاة» كان يعرض 7 من 8 كأن شرطاً ناقصاً

- **المُلاحظة:** `aggregates.conditions_covered` كان يعدّ الشروط ذات `status = core` فقط، فيظهر في التقرير وواجهة MORS ودليل اللجنة **«7 من 8»** — رقم يُقرأ على أنه شرط من شروط الهاكاثون الثمانية غير مُحقق، مع أن كل شرط له مصادر مُسنَدة وشيفرة مطابقة داخل المشروع.
- **الإصلاح:** `agents/sources_data.py:512` — `conditions_covered` يعرض الآن عدد الشروط التي لها `sources` **و**`used_in` (أي **8 من 8**)، ويُضاف معه `conditions_core = 7` و`conditions_extension = 1` حتى يبقى التقسيم الصادق ظاهراً (شرط الرؤية الحاسوبية مغطى على مستوى بيانات FITS/الصفائح والبكسل المُثمَّن مسجَّل كامتداد). حُدِّث معه نصّ اللجنة (`tools/build_committee.py:303`) والعقد (`docs/API_CONTRACT.md`).
- **التحقّق:** `conditions_covered == conditions_total (all 8 mapped)` + `conditions_core + conditions_extension == 8` + `core count == status=core count` + `exactly one extension condition`.

---

## 3) مشكّلات الجولات السابقة (مُوثّقة ومُصلَحة)

| # | المشكلة | الإصلاح | التحقّق |
|---|---|---|---|
| P-15 | `Kp = 0.0` كان يُعامَل كـ «لا قيمة» (`or -1`) فيفلتر البيانات الصحيحة | فحص صريح `is None` | `Kp within 0..9 scale` |
| P-16 | استعلام `neo` يعيد `items` بينما الفحص يقرأ `rows` | تطبيع إلى `items`/`rows` | `neo endpoint returns close approaches` |
| P-17 | `GM_earth` كنصّ بوحدة m³/s² (‎3.986004418e14‎) يستخدم مباشرة في صيغ كيلومترية → سرعات خاطئة | قراءة رقمية + تحويل `/1e9` | `velocity matches sqrt(GM/a) within 12%` |
| P-18 | الخادم القديم كان يُفتح بدل واجهة MORS | `START.bat` يفتح `/mors` + شارة `.mors-cta` في الصفحة القديمة | فحص HTML المُخدَم |
| P-19 | أرقام التقرير/الملفّات كانت قديمة بعد كل تعديل | إعادة تشغيل `POST /api/pipeline/run` ثم إعادة بناء `build_report` / `build_guide` / `build_committee` / `build_zip` | تطابق `last_report.json` مع `docs/*.pdf` |
| P-20 | نص عربي في PDFات reportlab يخرج مقطوعاً/معكوساً | `arabic_reshaper + python-bidi` + خطوط `Ar/ArB` + لفّ الأسطر يدوياً | عدّ معادلات العرض `0xFE70–0xFEFF` |

---

## 4) التحقّق الآلي — `tools/audit_data.py`

فحص للقراءة فقط (لا يكتب شيئاً) ينفّذ **225+ فحصاً** على الواجهات الحيّة (العدد الدقيق يتفاوت 225–227 حسب عدد بطاقات المشكلات النشطة)، مقسّمة إلى:

| المجموعة | ماذا يثبت |
|---|---|
| `audit_health` / `audit_datahealth` | صحة الخدمة ورياضيات جودة البيانات (خلايا، نِسَب، دقّة) |
| `audit_problems` / `audit_solutions` | مخطط البطاقات العشرة (`problem_id, title, severity, category, component, symptom, root_cause, fix, evidence, priority`) والمفاتيح الممنوعة، صيغة الأولوية وعتباتها، ربط الحلول بالمشكّلات |
| `audit_satellite` | كبلر: `T = 2π√(a³/μ)` و`v = √(μ/a)` مقابل القيم المُعلَنة (≤10–12%)، الفئات، الأوج/الحضيض |
| `audit_exoplanets` + `audit_exoplanet_physics` | `a³/T²` ضمن كتلة نجمية معقولة، بوابات S، تغطية `pl_insol`، حدود الكتلة/نصف القطر/السنة |
| `audit_objects` | صلاحية RA/Dec وشرح مواضع الكواكب |
| `audit_sky_surfaces` + `audit_curves` | اتساق التصوير الضوئي `2.5·log₁₀(max/min)`، المرحلة `[0,1)`، الطول `v = z·c`، ترتيب الأطوال الموجية |
| `audit_space_weather` + `audit_kp_daily` | عدّ CME مقابل FLR، مقياس OMNI، تطابيق سلاسل Kp اليومية مع قائمة الانبعاثات |
| `audit_quantum_physics` | تطبيع الحالات، إنتروبيا بيل `= 1`، تدرّج `ħω` متساوٍ، محور رابي، `cm⁻¹ = E/(hc)` |
| `audit_spectral_consistency` | `band_ids` مقابل `bands`، تنازل PCA ومجموع ≤ 1، مؤشرات `NDVI/NDVI/NDWI ∈ [-1,1]` |
| `audit_light_pollution` | اتجاه NELM مقابل مؤشّر الأضواء، بوابات Bortle 1–9، انحدار منحنى المرجع |
| `audit_horizons` | فصل مسارات الأجسام ومدى نصف القطر لكل جسم |
| `audit_images_fits` | روابط/تواريخ ISO/رؤوس FITS مرجعية لصور حقيقية |
| `audit_report` / `audit_sources` / `audit_team` / `audit_modules` | المجلس، مجموعات المصادر الستّ عشرة، **ربط الشروط الثمانية** (8/8 مغطاة: 7 أساسية + 1 توسعة)، قائمة الفريق (القائد فقط بالاسم)، 19 واجهة في فهرس MORS |
| `audit_prompts` | مكتبة المطالبات الستّ (`aimors_scientific`, `uiux_command_center`, `uiux_visualization`, `cyber_mors`, `analytics_report`, `deep_audit`) محمّلة بنصّ كامل وسِمة تتبّع |
| `audit_name_policy` | لا اسم لأي عضو غير القائد في أي حِمل API ولا في أي ملف من ملفات المشروع |

**ملاحظة:** ثلاث حالات فشل ظهرت أثناء بناء الفحص نفسه وأُصلحت في الفحص (ليست أعطال منتج): الاعتماد على `value or default` حيث `0.0` قيمة صحيحة، اتجاه NELM المقلوب في الافتراض، ودلالة `no_data` في الأيام التي تحمل انبعاثات بلا Kp.

---

## 5) فريق العمل

> سياسة الأسماء: **الاسم المنشور الوحيد هو قائد الفريق ليث رعد**؛ المقاعد الثلاثة الأخرى تُعرض بـ *الدور* الوظيفي فقط، فلا يظهر اسم أي عضو آخر في أي ملف أو واجهة أو تقرير داخل المشروع (فحص `no private member name in any project file`).

| المعرّف | الاسم / الدور | اللقب | الدور | يملك |
|---|---|---|---|---|
| TM-1 | **ليث رعد** | Team Leader | قائد الفريق — بنية النظام، التكامل، التسليم | `HOME.MORS`, `PROJECTS.MORS`, `TEAM.MORS`, `P-007` |
| TM-2 | Data Analyst | Data Analyst | امتصاص البيانات، جودة البيانات، تحليل المشكلات | `DATA.MORS`, `PROBLEMS.MORS`, `SOLUTIONS.MORS`, `P-002/003/004` |
| TM-3 | Astronomy Researcher | Astronomy Researcher | الفلك، الأرصاد، المصادر والمراجع | `ASTRONOMY.MORS`, `SOURCES.MORS`, `P-005/006/008` |
| TM-4 | Space Systems & AI Engineer | Space Systems & AI Engineer | الذكاء الاصطناعي، الأقمار، الحوسبة الكمّية | `AI.MORS`, `SATELLITE.MORS`, `QUANTUM.MORS`, `P-001` |

التحقق: `team is the four-member submission roster` + `only the team leader is published by name` + `no private member name in any API payload` + `no private member name in any project file` + `TM-1 is flagged as team leader` + `module ownership covers the 6 MORS domains`.

---

## 6) للإعادة بعد أي تعديل على الكود

```powershell
# 1) أعد تشغيل الخادم
Get-CimInstance Win32_Process -Filter "Name='python.exe'" |
  Where-Object { $_.CommandLine -like '*main.py*' -and $_.CommandLine -notlike '*selftest*' } |
  ForEach-Object { Stop-Process -Id $_.ProcessId -Force }
Start-Process -FilePath python -ArgumentList "main.py" -WorkingDirectory "<root>" -WindowStyle Hidden

# 2) حدّث بيانات العلم إن تغيّر agents/*.py (الواجهات science تُخدَم من آخر تشغيل)
Invoke-RestMethod -Method Post -Uri http://127.0.0.1:5000/api/pipeline/run -Body '{}' -ContentType application/json

# 3) تحقّق
python tools\audit_data.py          # → RESULT: AUDIT: PASS
python main.py --selftest           # → SELFTEST : PASS ✅

# 4) أعد بناء المخرجات إن تغيّرت الأرقام/الأسماء
python tools\build_report.py
python tools\build_guide.py
python tools\build_committee.py
python tools\build_zip.py           # → الحزمة النهائية + 00_MANIFEST.txt
```
